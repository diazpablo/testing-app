import React from 'react';
import ReactLoading from "react-loading";

const Loading = () => {
	return (
		<ReactLoading type="spin" color="#000" height={56} width={56} />
	);
};

export default Loading;
